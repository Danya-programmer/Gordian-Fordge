services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: gordian-backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./uploads:/app/uploads
    restart: unless-stopped
